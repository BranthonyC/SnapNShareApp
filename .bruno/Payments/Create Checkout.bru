meta {
  name: Create Checkout
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/events/{{eventId}}/checkout
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{hostToken}}
}

body:json {
  {
    "tier": "paid",
    "currency": "USD"
  }
}
