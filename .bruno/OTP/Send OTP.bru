meta {
  name: Send OTP
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/events/{{eventId}}/otp/send
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{guestToken}}
}

body:json {
  {
    "channel": "email",
    "destination": "guest@example.com"
  }
}
